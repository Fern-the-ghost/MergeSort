#include <iostream>
#include <cmath>
using namespace std;

struct Node {
  int data;
  Node* next;

Node(int newData) {
      data = newData;
      next = nullptr;
  }
};

class LinkList {
  Node *head;

  LinkList() {
    head = nullptr;
  }

void split (node* head,node*&a,node*&b) {
}

Node merge(node* a, node* b) {
}

Node mergeSort( node* head) {
}

};

int main() {
  Node list = new Node;
  return 0;
}
